{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings",
  "description": "Capability for the M6 settings window: app-command IPC, cross-window settings-changed events, and closing itself.",
  "windows": ["settings"],
  "permissions": [
    "core:default",
    "core:window:allow-close"
  ]
}
